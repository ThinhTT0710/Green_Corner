@model IEnumerable<EventDTO>
<div class="page-content pt-50">
    <div class="container">
        <div class="archive-header-2 text-center">
            <h1 class="display-2 mb-50">Events List</h1>
            <div class="row">
                <div class="col-lg-5 mx-auto">
                    <div class="sidebar-widget-2 widget_search mb-50">
                        <div class="search-form">
                            <form action="#">
                                <input type="text" placeholder="Search vendors (by name or ID)..." />
                                <button type="submit"><i class="fi-rs-search"></i></button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
       
        <div class="row vendor-grid">
            @foreach (var item in Model)
            {
                <div class="col-lg-6 col-md-6 col-12 col-sm-6">
                    <div class="vendor-wrap style-2 mb-40">
                    <div class="product-badges product-badges-position product-badges-mrg">
                        <span class="hot">@item.Status</span>
                    </div>
                    <div class="vendor-img-action-wrap">
                        <div class="vendor-img">
                            <a href="vendor-details-1.html">
                                <img class="default-img" src="./imgs/vendor/vendor-1.png" alt="" />
                            </a>
                        </div>
                        
                    </div>
                    <div class="vendor-content-wrap">
                        <div class="mb-30">
                                <h4 class="mb-5"><a asp-action="GetEventById" asp-route-eventId="@item.CleanEventId">@item.Title</a></h4>
                            <div class="vendor-info d-flex justify-content-between align-items-end mt-30">
                                <ul class="contact-infor text-muted">
                                    <li><strong>Description: </strong> <span>@item.Description</span></li>
                                    <li><strong>Max Participants:</strong><span>@item.MaxParticipants</span></li>
                                    <li><strong>Date:</strong><span>@item.StartDate</span></li>
                                </ul>
                                    <a style="margin-left:200px" asp-action="GetEventById" asp-route-eventId="@item.CleanEventId" class="btn btn-xs">View Detail<i class="fi-rs-arrow-small-right"></i></a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            }
        </div>

    </div>
</div>