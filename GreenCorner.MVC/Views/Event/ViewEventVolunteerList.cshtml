@model IEnumerable<EventVolunteerWithUserViewModel>
@{
    ViewData["Title"] = "Sự kiện";
}
<div class="card shadow border-0 mt-4">
    <div class="row">
        <h2 class="content-title card-title">Volunteer List</h2>
    </div>
    <div class="card-body p-4">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead class="table-light text-center">
                    <tr>
                        <th>Sự kiện</th>
                        <th>Họ tên</th>
                        <th>Trạng thái điểm danh</th>
                        <th class="text-end">Thao tác</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                            <tr class="align-middle text-center">
                                <td>@item.Volunteer.CleanEventId</td>
                                <td>@item.User.FullName</td>
                                <td>
                                @{
                                    var status = item.Volunteer.AttendanceStatus;
                                    if (string.IsNullOrEmpty(status))
                                    {
                                                    <span class="text-muted">Chưa điểm danh</span>
                                    }
                                    else if (status == "Present")
                                    {
                                                    <span class="badge bg-success">Có mặt</span>
                                    }
                                    else if (status == "Absent")
                                    {
                                                    <span class="badge bg-danger">Vắng mặt</span>
                                    }
                                    else
                                    {
                                                    <span>@status</span>
                                    }
                                }
                                </td>
                                <td class="text-end">
                                <button type="button"
                                        class="btn btn-outline-danger btn-sm"
                                        data-bs-toggle="modal"
                                        data-bs-target="#kickModal-@item.Volunteer.UserId">
                                    Loại
                                </button>
                                @if (item.Volunteer.AttendanceStatus == "Present")
                                {
                                <!-- Nút mở modal -->
                                <button type="button"
                                        class="btn btn-outline-success btn-sm"
                                        data-bs-toggle="modal"
                                        data-bs-target="#confirmModal-@item.Volunteer.UserId">
                                    Thưởng Điểm
                                </button>
                                }
                                </td>
                            </tr>
                        @if (item.Volunteer.AttendanceStatus == "Present")
                        {
                        <!-- Modal xác nhận thưởng điểm -->
                        <div class="modal fade" id="confirmModal-@item.Volunteer.UserId" tabindex="-1" aria-labelledby="modalLabel-@item.Volunteer.UserId" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <form asp-action="EarnPoints"
                                          asp-controller="PointTransaction"
                                          method="post">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="modalLabel-@item.Volunteer.UserId">Xác nhận thưởng điểm</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
                                        </div>
                                        <div class="modal-body">
                                            @{
                                                var calculatedPoints = item.Volunteer.IsTeamLeader == true ? item.Volunteer.PointsAwarded * 3 : item.Volunteer.PointsAwarded;
                                            }
                                            Bạn có chắc chắn muốn thưởng <strong>@calculatedPoints</strong> điểm cho tình nguyện viên <strong>@item.User.FullName</strong>?
                                        </div>
                                        <div class="modal-footer">
                                            <input type="hidden" name="userId" value="@item.Volunteer.UserId" />
                                            <input type="hidden" name="points" value="@calculatedPoints" />
                                            <input type="hidden" name="eventId" value="@item.Volunteer.CleanEventId" />
                                            <button type="button" class="btn btn-secondary" style="border-radius: 10px;" data-bs-dismiss="modal">
                                                Hủy
                                            </button>
                                            <button type="submit" class="btn btn-success" style="font-weight: 700;font-size: 14px;padding: 12px 30px;">
                                                Xác nhận
                                            </button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                        }
                        <!-- Modal xác nhận loại tình nguyện viên -->
                        <div class="modal fade" id="kickModal-@item.Volunteer.UserId" tabindex="-1" aria-labelledby="kickModalLabel-@item.Volunteer.UserId" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <form asp-action="KickVolunteer"
                                          asp-controller="Admin"
                                          method="post">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="kickModalLabel-@item.Volunteer.UserId">Xác nhận loại tình nguyện viên</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Đóng"></button>
                                        </div>
                                        <div class="modal-body">
                                            Bạn có chắc chắn muốn <strong>loại</strong> tình nguyện viên <strong>@item.User.FullName</strong> khỏi sự kiện?
                                        </div>
                                        <div class="modal-footer">
                                            <input type="hidden" name="userId" value="@item.Volunteer.UserId" />
                                            <input type="hidden" name="eventId" value="@item.Volunteer.CleanEventId" />
                                            <button type="button" class="btn btn-secondary" style="border-radius: 10px;" data-bs-dismiss="modal">Hủy</button>
                                            <button type="submit" class="btn btn-danger" style="font-weight: 700;font-size: 14px;padding: 12px 30px;">Xác nhận loại</button>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>

                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


